import React from "react";
import ReactDOM from "react-dom/client";
import RulesetConsole from "./RulesetConsole.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <RulesetConsole />
  </React.StrictMode>
);
